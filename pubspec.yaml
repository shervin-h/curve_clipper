name: curve_clipper
description: "A Flutter package for cutting widgets into wavy, wavy sin, wavy curtain,  convex, concave, and... shapes"
version: 0.1.0
homepage: https://github.com/shervin-h/curve_clipper
repository: https://github.com/shervin-h/curve_clipper
issue_tracker: https://github.com/shervin-h/curve_clipper/issues

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
